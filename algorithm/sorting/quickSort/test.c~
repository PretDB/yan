#include <stdio.h>
#include <stdlib.h>

#define SWAP(A,B) (int tmp = 0; tmp = A; A = B; B = tmp;)

int main(int argc, char** argv)
{
	int a = 15, b = 20;

	SWAP(a,b);

	printf("a = %d, b = %d\n", a, b);


}
