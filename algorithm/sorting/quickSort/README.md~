# Quick Sorting

pseudo code

function quickSorting(data, length)
	while(&large < &small)
		pick large and small
		swap large and small
	swap small and data
	quickSorting(data, leftLength)
	quickSorting(large, rightLength)
